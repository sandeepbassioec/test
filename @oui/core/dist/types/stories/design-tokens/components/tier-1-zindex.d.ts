import './token-specimen';
declare class Tier1ZIndex extends HTMLElement {
    private tokens;
    constructor();
    connectedCallback(): void;
    private loadTokens;
    private filterTokens;
    private renderTokenGroup;
    private render;
}
export default Tier1ZIndex;
